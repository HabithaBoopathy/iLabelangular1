import { ExecutiveFilterPipe } from '../filters/executive-filter.pipe';

describe('ExecutiveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExecutiveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
